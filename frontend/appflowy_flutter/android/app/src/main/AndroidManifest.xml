<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
  <application android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:networkSecurityConfig="@xml/network_security_config"
      android:enableOnBackInvokedCallback="true"
    android:name="${applicationName}">
    <activity android:name=".MainActivity"
      android:exported="true"
      android:launchMode="singleTop"
      android:theme="@style/LaunchTheme"
      android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
      android:hardwareAccelerated="true" android:windowSoftInputMode="adjustResize">
      <!--
			Specifies an Android theme to apply to this Activity as soon as
			the Android process has started. This theme is visible to the user
			while the Flutter UI initializes. After that, this theme continues
			to determine the Window background behind the Flutter UI.
			-->
      <meta-data android:name="io.flutter.embedding.android.NormalTheme"
        android:resource="@style/NormalTheme" />
      <!--
			Displays an Android View that continues showing the launch screen
			Drawable until Flutter paints its first frame, then this splash
			screen fades out. A splash screen is useful to avoid any visual
			gap between the end of Android's launch screen and the painting of
			Flutter's first frame.
			-->
      <meta-data android:name="io.flutter.embedding.android.SplashScreenDrawable"
        android:resource="@drawable/launch_background" />
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
      <meta-data android:name="flutter_deeplinking_enabled" android:value="true" />
      <intent-filter android:autoVerify="false">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data
            android:scheme="https"
            android:host="www.xiaomabiji.com"
            android:pathPrefix="/share" />
        <data
            android:scheme="http"
            android:host="www.xiaomabiji.com"
            android:pathPrefix="/share" />
      </intent-filter>
      <!-- Keep invite links on HTTPS so the Flutter handler can validate the
           trusted web origin before processing the invitation. -->
      <intent-filter android:autoVerify="false">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data
            android:scheme="https"
            android:host="www.xiaomabiji.com"
            android:pathPrefix="/invited" />
        <data
            android:scheme="http"
            android:host="www.xiaomabiji.com"
            android:pathPrefix="/invited" />
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="ponynotes" />
        <data android:host="note" />
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="ponynotes" />
        <data android:host="open" />
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="ponynotes" />
        <data android:host="invite" />
      </intent-filter>

    </activity>
    <!--
		Don't delete the meta-data below.
		This is used by the Flutter tool to generate GeneratedPluginRegistrant.java
		-->
    <meta-data android:name="flutterEmbedding" android:value="2" />
    <!-- Keep lifecycle bookkeeping, but defer SDK work until privacy consent. -->
    <provider
        android:name="androidx.startup.InitializationProvider"
        android:authorities="${applicationId}.androidx-startup"
        tools:node="merge">
      <meta-data android:name="androidx.emoji2.text.EmojiCompatInitializer" tools:node="remove" />
      <meta-data android:name="okhttp3.internal.platform.PlatformInitializer" tools:node="remove" />
      <meta-data android:name="androidx.profileinstaller.ProfileInstallerInitializer" tools:node="remove" />
    </provider>
    <meta-data android:name="io.flutter.embedding.android.EnableImpeller"
      android:value="false" />

    <!-- Required by super_clipboard to share binary image data with other apps. -->
    <provider
        android:name="com.superlist.super_native_extensions.DataProvider"
        android:authorities="${applicationId}.SuperClipboardDataProvider"
        android:exported="true"
        android:grantUriPermissions="true" />

    <!-- WeChat 微信登录回调 Activity：微信 SDK 完成授权后会回调这个 Activity
         必须使用与 SDK 文档一致的 taskAffinity（填包名，不是 .wxapi 后缀），
         否则 Android 12+ 上 SDK 找不到 WXEntryActivity 导致回调不到。

         **关键坑**：微信 SDK 内部硬编码查找的是完整类名
         `${applicationId}.wxapi.WXEntryActivity`，所以即使我们的 WXEntryActivity
         实际在 `.wechat.` 包下，也必须用 <activity-alias> 给它起一个
         `${applicationId}.wxapi.WXEntryActivity` 的别名，否则 SDK 找不到，
         回调 Intent 永远不会进 WXEntryActivity。-->
    <activity
        android:name=".wechat.WXEntryActivity"
        android:enabled="false"
        android:exported="true"
        android:launchMode="singleTask"
        android:taskAffinity="${applicationId}"
        android:excludeFromRecents="true"
        android:theme="@android:style/Theme.Translucent.NoTitleBar" />

    <!-- activity-alias：让 SDK 硬编码查找的 `${applicationId}.wxapi.WXEntryActivity`
         路径指向我们真实的 WXEntryActivity。这是微信 SDK 在 Android 上的硬性要求。

         注意：fluwx 插件自己也声明了同名 alias（target 是 FluwxWXEntryActivity），
         必须用 tools:replace 强制覆盖，否则 manifest 合并会失败。-->
    <activity-alias
        android:name="${applicationId}.wxapi.WXEntryActivity"
        android:enabled="false"
        android:exported="true"
        android:launchMode="singleTask"
        android:targetActivity=".wechat.WXEntryActivity"
        tools:replace="android:launchMode,android:targetActivity" />

    <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
    <!-- Enabled only while the consented user has a pending local reminder. -->
    <receiver
        android:name=".ReminderBootReceiver"
        android:enabled="false"
        android:exported="false">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED"/>
        <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
        <action android:name="android.intent.action.QUICKBOOT_POWERON" />
        <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
      </intent-filter>
    </receiver>
    <receiver
        android:name=".ReminderRestoreCleanupReceiver"
        android:enabled="true"
        android:exported="false" />

    <!-- Login/payment callbacks remain installed, but cannot associate-start the
         app before privacy consent. AndroidPrivacyConsent enables them after consent. -->
    <activity
        android:name="com.postliu.douyin_login.DouyinCallbackActivity"
        android:enabled="false" />
    <activity-alias
        android:name="${applicationId}.douyinapi.DouYinEntryActivity"
        android:enabled="false" />
    <!-- fluwx is used only by iOS in this project. Android login uses the local
         WeChatBridge and Android payments use Alipay, so remove its callbacks. -->
    <activity
        android:name="com.jarvan.fluwx.wxapi.FluwxWXEntryActivity"
        tools:node="remove" />
    <activity-alias
        android:name="${applicationId}.wxapi.WXPayEntryActivity"
        tools:node="remove" />
    <provider
        android:name="com.jarvan.fluwx.FluwxFileProvider"
        tools:node="remove" />
    <activity
        android:name="com.alipay.sdk.app.PayResultActivity"
        android:enabled="false" />
    <activity
        android:name="com.alipay.sdk.app.AlipayResultActivity"
        android:enabled="false" />

    <!-- Android purchases use Alipay/WeChat. The generic in_app_purchase
         dependency is retained for Apple platforms, so remove its unused
         Android billing and telemetry entry points from this APK. -->
    <meta-data
        android:name="com.google.android.play.billingclient.version"
        tools:node="remove" />
    <activity
        android:name="com.android.billingclient.api.ProxyBillingActivity"
        tools:node="remove" />
    <activity
        android:name="com.android.billingclient.api.ProxyBillingActivityV2"
        tools:node="remove" />
    <service
        android:name="com.google.android.datatransport.runtime.backends.TransportBackendDiscovery"
        tools:node="remove" />
    <service
        android:name="com.google.android.datatransport.runtime.scheduling.jobscheduling.JobInfoSchedulerService"
        tools:node="remove" />
    <receiver
        android:name="com.google.android.datatransport.runtime.scheduling.jobscheduling.AlarmManagerSchedulerBroadcastReceiver"
        tools:node="remove" />

  </application>
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <!-- Required only while ScheduledNotificationBootReceiver is dynamically enabled. -->
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
  <uses-permission android:name="com.android.vending.BILLING" tools:node="remove" />
  <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:node="remove" />
  <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" tools:node="remove" />
  <uses-permission android:name="android.permission.READ_CALENDAR" />
  <uses-permission android:name="android.permission.WRITE_CALENDAR" />
  <!-- Permission to read files from external storage (outside application container).
  As of Android 12 this permission no longer has any effect. Instead use the
  READ_MEDIA_IMAGES, READ_MEDIA_VIDEO or READM_MEDIA_AUDIO permissions. -->
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
    android:maxSdkVersion="32" />
  <!-- Permissions to read media files. -->
  <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
  <queries>
    <intent>
      <action android:name="android.support.customtabs.action.CustomTabsService" />
    </intent>
  </queries>
  <!--
    Media access permissions.
    Android 13 or higher.
    Used for VideoBlock (edia_kit)
  -->
  <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
  <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
  <uses-permission android:name="android.permission.CAMERA" />

  <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
  <queries>
    <intent>
      <action android:name="android.intent.action.PROCESS_TEXT"/>
      <data android:mimeType="text/plain"/>
    </intent>
    <!-- WeChat SDK Configuration -->
    <package android:name="com.tencent.mm" />
    <!-- Exact visibility for the Douyin installation check; do not enumerate apps. -->
    <package android:name="com.ss.android.ugc.aweme" />
    <!-- Exact visibility for Tobias.isAliPayInstalled. -->
    <package android:name="com.eg.android.AlipayGphone" />
  </queries>
</manifest>
